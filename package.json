{
  "name": "festivalmusica",
  "version": "1.0.0",
  "description": "practica",
  "type": "module",
  "description1": "script sasss compila automaticamente cada vez que se guarda un archivo en la carpeta scss, y crea el archivo compilado en dist/css --watch se usa para que ejecute continuamente el script",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch src/scss:build/css",
    "dev": "gulp dev"
  },
  "repository": {
    "type": "git",
    "url": "git remote add origin https://github.com/dcduter/festival.git"
  },
  "keywords": [
    "sass",
    "npm",
    "gulp"
  ],
  "author": "juan",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^5.0.0",
    "gulp-sass": "^6.0.1",
    "sass": "^1.86.0"
  }
}
